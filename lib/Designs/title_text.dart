import 'package:flutter/material.dart';

TextStyle TitleTextSyle(BuildContext context) => TextStyle(
      fontFamily: "Lugrasimo",
      fontSize: 40,
      color: Theme.of(context).colorScheme.primary,
      fontWeight: FontWeight.bold,
    );
